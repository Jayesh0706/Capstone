Edit my existing files in infra/envs/dev.

1. In main.tf, add a locals block at the top:
   locals {
     tags = {
       project     = "primarydiagnosis"
       environment = "dev"
       managed_by  = "terraform"
     }
   }

2. In the existing "storage" module call, add this argument:
   tags = local.tags
   Also change its name_prefix argument to "primaryddev"

3. In the existing "keyvault" module call, add this argument:
   tags = local.tags
   Also change its name_prefix argument to "primaryddev"

Do not change anything else. Show me the full updated main.tf.
