Edit main.tf in infra/modules/storage. Keep everything already there 
(the storage account, the existing "cases" container, the queue) - do 
not remove or change them. Add one new resource:

resource "azurerm_storage_container" "charts" {
  name                  = "charts"
  storage_account_id    = azurerm_storage_account.cases.id
  container_access_type = "private"
}

Show me the full updated main.tf so I can confirm nothing else changed.
