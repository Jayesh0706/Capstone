Create three Terraform files in my current folder infra/modules/keyvault.

File 1, variables.tf, declaring input variables:
- name_prefix (string)
- location (string)
- resource_group_name (string)
- tenant_id (string)
- admin_object_id (string)
Also add a required_providers block requiring azurerm ~> 4.0. 
Do NOT add a provider configuration block.

File 2, main.tf, that creates an azurerm_key_vault:
- name = name_prefix plus "kv" (must be 3 to 24 chars, lowercase 
  alphanumeric and hyphens)
- location and resource_group_name from variables
- tenant_id from variable
- sku_name = "standard"
- IMPORTANT: use access policy mode, NOT RBAC. Set 
  enable_rbac_authorization = false
- add one access_policy block granting the admin_object_id these 
  secret permissions: Get, List, Set, Delete, Purge
- set the access_policy tenant_id to the tenant_id variable

File 3, outputs.tf, outputting:
- key_vault_id (the vault id)
- key_vault_uri (the vault uri)

Create only these three files. Do not configure a provider. 
Do not create a resource group. Do not create any secrets yet.

Show me the full content of each file and note any assumptions.
