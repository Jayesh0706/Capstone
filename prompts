I have an Azure Container App named "stage-backend" (resource group 
"GENAI-B5-Project-T3-RG") that is stuck. History:

- It was created via Terraform with a public placeholder image 
  (mcr.microsoft.com/azuredocs/containerapps-helloworld:latest)
- Multiple terraform apply attempts to update it to a new image 
  (stageb5t3acr.azurecr.io/backend:placeholder) have failed with 
  either "Operation expired" after ~20 minutes, or 
  "ContainerAppOperationInProgress" 409 conflicts
- `az containerapp revision list` for this app currently returns 
  completely empty - no revisions exist at all, despite the app 
  resource itself existing
- I do NOT have permission to delete Azure resources (company policy) 
  - do not suggest az containerapp delete or terraform destroy for 
  this resource under any circumstances

Please:
1. Check `az containerapp show --name stage-backend --resource-group 
   GENAI-B5-Project-T3-RG --query "properties.provisioningState"` and 
   report the actual current state
2. Check `az containerapp revision list --name stage-backend 
   --resource-group GENAI-B5-Project-T3-RG -o table` and report if 
   anything shows now
3. Based on those two results, tell me what's actually happening and 
   what non-destructive options exist to fix it (e.g. az containerapp 
   update directly, waiting longer, or something else) - do not run 
   any command that deletes, destroys, or replaces the resource without 
   explaining what it does first and getting my explicit confirmation
