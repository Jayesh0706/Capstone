In infra/envs/stage/main.tf, the "module container_apps" block currently only passes 
these 4 arguments: source, name_prefix, location, resource_group_name, tags.

Add the following additional arguments to this same module block, so it passes stage's 
own resource values instead of falling back to the module's dev-matching defaults:

  key_vault_uri                   = module.keyvault.key_vault_uri
  azure_storage_account_url       = module.storage.storage_account_url
  azure_doc_intelligence_endpoint = module.docintel.docintel_endpoint
  cosmos_endpoint                 = module.cosmos.cosmos_endpoint
  acr_login_server                = module.registry.acr_login_server
  acr_username                    = "stageb5t3acr"
  acr_password_secret_name        = "acr-password"

Do NOT add neo4j_uri, neo4j_user, azure_foundry_project_endpoint, foundry_inference_endpoint, 
or backend_base_url — these should intentionally fall back to the module's existing defaults 
for now (stage shares dev's Neo4j and Foundry endpoints as a documented decision).

Verify the module.storage, module.docintel, module.cosmos, and module.registry references 
above use output names that actually exist in those modules' outputs.tf files — check each 
one before assuming the name is correct, and tell me if any of these output names don't match.

Do NOT modify any other file. Do NOT touch infra/envs/dev/main.tf.
