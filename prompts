I want to write my own Dockerfile for the backend service, from scratch, 
for a stage environment pipeline I'm building. Please:

1. First, read backend/requirements.txt (or package.json, whichever 
   applies) and identify the actual runtime, dependencies, entrypoint 
   file, and the port the app listens on.

2. As a reference only (do not copy it verbatim), also read the existing 
   backend/Dockerfile so I understand what pattern dev's setup already 
   uses - but build mine based on the app's actual requirements, not by 
   duplicating that file.

3. Write a NEW file at backend/Dockerfile.stage (do not modify or delete 
   the existing backend/Dockerfile). Use a multi-stage build if it's 
   appropriate for this app (smaller final image, separate build/runtime 
   stages). 

4. As you write each instruction (FROM, WORKDIR, COPY, RUN, EXPOSE, CMD, 
   etc.), explain in plain language why it's needed and what it does - 
   I want to understand this file, not just have Copilot generate it 
   silently.

5. Do the same for the frontend service: read its actual requirements, 
   reference (don't copy) its existing Dockerfile if one exists, and 
   write a new file at frontend/Dockerfile.stage with the same 
   instruction-by-instruction explanation.

After both files are created, summarize: what base images did you choose 
and why, what port does each app expose, and what would the exact 
`docker build` command look like to build each one locally for testing.
