Edit infra/modules/container_apps/main.tf. In BOTH the backend and 
frontend azurerm_container_app resources, add a secret block that 
supplies the ACR registry password. The secret name must exactly match 
what the registry block's password_secret_name references, which is:
"primaryddevacrazurecrio-primaryddevacr"

Add to both apps:

secret {
  name                = "primaryddevacrazurecrio-primaryddevacr"
  key_vault_secret_id = "https://primaryddevkv.vault.azure.net/secrets/acr-password"
  identity            = "System"
}

Keep all existing secret and env blocks. Show me the file.
