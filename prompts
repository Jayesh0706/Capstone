Good — this is about **communicating the security work clearly**, not just doing more of it. Let me give you the complete plan: what to actually implement (quick wins), what to document (the honest tradeoffs), and how to present it so the team/graders see deliberate security thinking.

---

## Part A — The quick wins to actually implement (do these first)

### A1. Tighten CORS

```
git checkout dev
git pull origin dev
git checkout -b feature/security-hardening
```

```
Edit infra/modules/container_apps/main.tf. In the BACKEND container's 
env blocks, change CORS_ORIGINS from "*" to the frontend's URL: 
"https://primaryddev-frontend.gentlepond-2a87df60.eastus.azurecontainerapps.io"
Show me the updated env block.
```

### A2. Restrict Cosmos DB firewall (if feasible)

Check current outbound IPs of your Container Apps environment:
```
az containerapp env show --name primaryddev-env --resource-group GENAI-B5-Project-T3-RG --query "properties.staticIp" -o tsv
```

If Container Apps doesn't give a stable static IP easily, the simpler Azure-native option is enabling **"Allow access from Azure services"** on Cosmos, which restricts access to Azure's own network rather than the open internet:
```
az cosmosdb update --name primaryddev-cosmos --resource-group GENAI-B5-Project-T3-RG --enable-virtual-network true --networkAclBypass AzureServices
```
Or handle this the same way via Terraform (add `public_network_access_enabled` + `network_acl_bypass_for_azure_services` to the Cosmos module) — tell me if you want that as a Terraform prompt instead of a CLI command, since we prefer code-based changes.

### A3. Backend internal-only ingress (the bigger one, from our last exchange)

Confirm with developer first (does anything besides frontend need direct backend access?), then follow the 9 steps from my previous message (ingress → internal, output the internal FQDN, wire frontend's `BACKEND_BASE_URL` to it, plan, apply, verify old URL is unreachable, verify frontend still works).

### A4. Plan, apply, verify, commit

```
cd infra\envs\dev
terraform plan
```
Review carefully. Then:
```
terraform apply
```
Verify: old backend public URL fails, frontend still works end-to-end, Cosmos still reachable from backend.

```
cd ../../..
git add infra/
git commit -m "Security hardening: restrict CORS, backend internal-only ingress, Cosmos network restriction"
git push -u origin feature/security-hardening
```
→ PR to dev.

---

## Part B — Document what you did AND what you deliberately deferred

This is the part that actually shows "we worked on security" to anyone reviewing later — implementation alone doesn't communicate reasoning; the write-up does.

Here's the complete plan — implementation steps plus a document that shows the reasoning.

## Part A — Implement (steps above): CORS tightening, Cosmos network restriction, backend internal-only ingress, then plan/apply/verify/commit/PR.

## Part B — `security-notes-2026-08-18.md`

This is the piece that actually **communicates** the work — implementation alone is invisible to a reviewer unless it's written down. The doc has two halves:

**"Implemented"** — six concrete things, each with a one-line "why," including the CORS/ingress/Cosmos changes you're about to make.

**"Deliberately deferred"** — this is the important half. It covers VNet/private endpoints, Neo4j Aura's network limits, the no-RBAC key-based-auth constraint, shared API key vs. per-user auth, and no WAF — **each with a stated reason**, not just "not done." This is what separates "we ran out of time" from "we evaluated this and made a deliberate call given our constraints."

That framing — implemented + reasoned deferrals — is exactly what reads as mature security thinking to a grader or teammate, rather than either overclaiming ("fully secure!") or looking incomplete.

## How to actually "let them know"

1. **Fill in the two `[bracketed]` placeholders** in the doc once you've confirmed the Cosmos restriction you actually applied and whether Neo4j Aura supports IP restriction
2. **Commit it** alongside the code changes in the same PR — `infra/docs/security-notes-2026-08-18.md`
3. **Reference it in the PR description**: "Security hardening pass — see `security-notes-2026-08-18.md` for what changed and what's deliberately deferred with reasoning"
4. **Send the team a short message** pointing at it:
   ```
   Did a security pass on infra: CORS locked down, backend no longer 
   publicly reachable (frontend-only now), Cosmos network restricted. 
   Full writeup + what we're deliberately deferring (and why) in 
   security-notes-2026-08-18.md — worth a read before demo.
   ```

Run Part A's steps, fill in the two placeholders based on what actually happens, then commit both the code and the doc together.
