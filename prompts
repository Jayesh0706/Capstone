Edit Terraform files in two module folders.

In infra/modules/storage:
- In variables.tf, add a variable "tags" of type map(string) with 
  default {}
- In main.tf, add "tags = var.tags" to the azurerm_storage_account 
  resource (storage containers and queues do not take tags, leave 
  those alone)

In infra/modules/keyvault:
- In variables.tf, add a variable "tags" of type map(string) with 
  default {}
- In main.tf, add "tags = var.tags" to the azurerm_key_vault resource

Show me the full updated variables.tf and main.tf for both modules.
